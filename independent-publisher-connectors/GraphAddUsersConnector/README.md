# Microsoft Graph Add Users Connector (Independent Publisher)

This connector creates users in Microsoft Entra using Microsoft Graph API.

## Publisher
Your Name

## Prerequisites
- Azure App registration with `User.ReadWrite.All`
- Client ID and Secret (via environment variable `tf_Secret_Id`)

## Authentication
Uses Azure AD OAuth 2.0 with `v2.0` endpoints.

## Supported operations
- Create User
